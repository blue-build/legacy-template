# image will be published to ghcr.io/<user>/<name>
name: mykinoite
# description will be included in the image's metadata
description: This is my personal OS image based on ublue-os/kinoite-main.

# the base image to build on top of (FROM) and the version tag to use
base-image: ghcr.io/ublue-os/kinoite-main
image-version: 39 # latest is also supported if you want new updates ASAP

# module configuration, executed in order
modules:
  # https://universal-blue.org/tinker/modules/files/
  - type: files
    files:
      - usr: /usr

  # https://universal-blue.org/tinker/modules/rpm-ostree/
  - type: rpm-ostree
    install:
      - zsh
      - virt-manager
      - bismuth
      - tldr
    remove:
      - firefox # default firefox removed in favor of flatpak
      - firefox-langpacks # langpacks needs to also be removed to prevent dependency problems

  # https://universal-blue.org/tinker/modules/bling/
  - type: bling # configure what to pull in from ublue-os/bling
    install:
      - justfiles
      - nix-installer # shell shortcuts for determinate system's nix installers
      - laptop # installs TLP and configures your system for laptop usage
      
  # https://universal-blue.org/tinker/modules/script/
  - type: script
    scripts:
      # this sets up the proper policy & signing files for signed images to work
      - signing.sh
